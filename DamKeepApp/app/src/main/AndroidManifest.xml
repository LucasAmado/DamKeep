<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lucasamado.damkeepapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".common.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.notas.detalle.EditarNotaActivity"></activity>
        <activity android:name=".ui.notas.detalle.NotaDetalleActivity" />
        <activity
            android:name=".ui.notas.lista.ListaNotasActivity"
            android:label="@string/title_activity_lista_notas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>